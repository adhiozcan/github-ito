<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Navigation view theme -->
    <style name="ThemeOverlay.AppTheme.NavigationView" parent="ThemeOverlay.AppCompat.Dark">
        <!-- This is the menu item text color. -->
        <item name="android:textColorPrimary">@color/colorWhite</item>
        <!-- This is the menu header text color. -->
        <item name="android:textColorSecondary">@color/colorWhite</item>
        <!-- This is the selected color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <!-- Universal dialog view theme-->
    <style name="UniversalDialog" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>
</resources>
